'use strict';

var React = require('react');
var Project = require('./Project.jsx');

var ProjectView;

module.exports = ProjectView = React.createClass({
  render: function () {
    return (
      <Project />
    );
  }
});
